{
    "model_name": "distilbert_128_signed",
    "build_from_scratch": false,
    "batch_size": 8,
    "epochs": 10,
    "learning_rate": 1e-3,
    "tokenizer": "distilbert-base-uncased",
    "max_length": 128
}
