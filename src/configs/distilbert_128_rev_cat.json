{
    "model_name": "distilbert_128_rev_cat",
    "build_from_scratch": false,
    "batch_size": 8,
    "epochs": 20,
    "learning_rate": 1e-3,
    "tokenizer": "distilbert-base-uncased",
    "max_length": 128
}
